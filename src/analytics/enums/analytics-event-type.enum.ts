/** Stored in PostgreSQL as the `analytics_event_type` enum type (Issue #7). */
export enum AnalyticsEventType {
  RECOMMENDATION_IMPRESSION = 'RECOMMENDATION_IMPRESSION',
  RECOMMENDATION_SELECTED = 'RECOMMENDATION_SELECTED',
}
